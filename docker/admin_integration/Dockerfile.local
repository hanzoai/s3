FROM alpine:latest

# Install required packages
RUN apk add --no-cache \
    ca-certificates \
    fuse \
    curl \
    jq

# Copy our locally built binary
COPY s3-local /usr/bin/s3
RUN chmod +x /usr/bin/s3

# Create working directory
WORKDIR /data

# Default command
ENTRYPOINT ["/usr/bin/s3"] 