/*
Package sqlite is for sqlite filer store.

The referenced "github.com/hanzoai/sqlite" driver (pure-Go path wraps modernc.org/sqlite) is too big when compiled.
So this is only compiled in "make full_install".
*/
package sqlite
