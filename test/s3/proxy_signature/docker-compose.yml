services:
  hanzo:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      /usr/bin/s3 mini
      -s3.config=/etc/hanzo/s3.json
      -s3.externalUrl=http://localhost:9000
      -ip=hanzo
    volumes:
      - ./s3.json:/etc/hanzo/s3.json:ro
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://hanzo:8333/status"]
      interval: 3s
      timeout: 2s
      retries: 20
      start_period: 5s

  nginx:
    image: nginx:alpine
    ports:
      - "9000:9000"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      hanzo:
        condition: service_healthy
